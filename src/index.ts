import { TailwindSelect } from './tailwindSelectCustom';
import { Prefix, Suffix, Option, NoOptions, Placeholder, Value } from '@cristianmpx/react-custom-select';
import type { SelectObjectRef, OptionObjectRef, OptionProps, SelectProps, AddonProps, AddonObjectRef, PlaceholderObjectRef, PlaceholderProps, ValueProps, ValueObjectRef } from '@cristianmpx/react-custom-select';

export {
    TailwindSelect, 
    Prefix,
    Suffix,
    Option,
    NoOptions,
    Placeholder,
    Value
};

export type {
    SelectObjectRef,
    SelectProps,
    OptionObjectRef,
    OptionProps,
    AddonProps,
    AddonObjectRef,
    PlaceholderObjectRef,
    PlaceholderProps,
    ValueProps,
    ValueObjectRef
}